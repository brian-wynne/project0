This project was written by Brian Wynne.

The index.html in the root of this project is the "homepage"

The CSS directory will contain all .css and .scss files. There is currently only 1 main file
to handle all webpages.

The Images directory contains all images being used by the website.

The Pages directory contains the rest of the .html pages. Each page was designed to show css and html working
togethor.